<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net46</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <!-- Private=False means do not copy to output. We can't because they have the same file name. -->
    <ProjectReference Include="..\ClassLibrary1\ClassLibrary1.csproj" Private="False"/>
    <ProjectReference Include="..\ClassLibrary2\ClassLibrary2.csproj" Private="False" />
  </ItemGroup>

</Project>
